**GuideCard** — "Guides for parents" article card.

```jsx
<GuideCard category="Costs" title="Understanding the Child Care Subsidy" readTime="6 min read" seed={1} />
```

Gradient thumbnail + uppercase category eyebrow + title + read-time. Renders as an `<a>`.
