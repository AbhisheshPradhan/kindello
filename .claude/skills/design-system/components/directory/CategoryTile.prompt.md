**CategoryTile** — "Browse by type" tile.

```jsx
<CategoryTile icon="blocks" label="Montessori" count={142} tone="coral" />
```

Tinted icon square + label + optional count. Tones: `teal` `coral` `sun`. Lay out in a responsive grid.
