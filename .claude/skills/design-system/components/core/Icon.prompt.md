**Icon** — inline Lucide glyph, no CDN dependency.

```jsx
<Icon name="map-pin" size={16} />
```

Stroke-based, inherits `currentColor`. Names match the bundled Lucide subset (star, map-pin, clock, users, phone, heart, shield-check, sparkles, search, baby, graduation-cap, blocks, sun, …).
