**PromptChip** — suggested-prompt pill under the hero search.

```jsx
<PromptChip onClick={...}>Open weekends</PromptChip>
```

White pill, hairline border; hovers to a teal tint. Lay several out in a wrapped flex row with `gap`.
