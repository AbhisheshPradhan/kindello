**StarRating** — sunny-yellow stars + score + review count.

```jsx
<StarRating value={4.8} count={126} />
<StarRating value={4} showValue={false} size={14} />
```

Supports decimal half-fill. Hide the text with `showValue={false}` for compact card corners.
