**Tag** — small rounded attribute pill.

```jsx
<Tag tone="teal">Montessori</Tag>
<Tag>Outdoor space</Tag>
```

Tones: `neutral` (default), `teal`, `coral`, `sun`. Use neutral for most attributes; reserve coral/teal for emphasis.
